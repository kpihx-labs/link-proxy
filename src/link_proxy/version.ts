/**
 * link-proxy — Version utility.
 *
 * PLACEHOLDER: Single source of truth for version from package.json.
 */

export const VERSION = "0.1.2";

export function getVersion(): string {
  // PLACEHOLDER: Read version from package.json
  return VERSION;
}
